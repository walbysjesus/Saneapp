import 'package:flutter/material.dart';

class IndustrialCleaningPage extends StatelessWidget {
  const IndustrialCleaningPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Limpieza industrial')),
      body: const Center(child: Text('Pantalla de limpieza industrial')), 
    );
  }
}

