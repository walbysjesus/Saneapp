import 'package:flutter/material.dart';

class CollectionTransportPage extends StatelessWidget {
  const CollectionTransportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RecolecciÃ³n y Transporte')),
      body: const Center(
        child: Text('Pantalla de RecolecciÃ³n y Transporte'),
      ),
    );
  }
}

