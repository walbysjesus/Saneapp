import 'package:flutter/material.dart';

class ConsultingPage extends StatelessWidget {
  const ConsultingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ConsultorÃ­a y Permisos Ambientales')),
      body: const Center(
        child: Text('Pantalla de ConsultorÃ­a y Permisos Ambientales'),
      ),
    );
  }
}

