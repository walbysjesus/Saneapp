import 'package:flutter/material.dart';

class VactorEquipmentPage extends StatelessWidget {
  const VactorEquipmentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Equipos vactor y succiÃ³n')),
      body: const Center(child: Text('Pantalla de equipos vactor y succiÃ³n')), 
    );
  }
}

