import 'package:flutter/material.dart';

class OtherEnvironmentalPage extends StatelessWidget {
  const OtherEnvironmentalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Otros servicios ambientales')),
      body: const Center(child: Text('Pantalla de otros servicios ambientales')), 
    );
  }
}

